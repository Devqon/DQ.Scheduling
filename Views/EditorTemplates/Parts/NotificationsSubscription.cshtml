@model DQ.Scheduling.Models.NotificationsSubscriptionPart

@*
    TODO: integrate other subscriptions: choose from subscription type?
*@


@if (WorkContext.CurrentUser == null) {
    <fieldset>
        @Html.LabelFor(m => m.Email, T("Email"))
        @Html.TextBoxFor(m => m.Email, new { type = "email" })
    </fieldset>
}